/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
      unoptimized: true,
    },
    // Otras configuraciones que puedas tener...
  }
  
  module.exports = nextConfig